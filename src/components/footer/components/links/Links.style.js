import { themeTimber } from '../../../../constants/colors';

export default theme => ({
  typography: {
    textTransform: 'uppercase',
    fontSize: '1rem',
    fontWeight: '600',
    color: themeTimber,
  },
});
